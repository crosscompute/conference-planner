---
crosscompute: 0.9.3
name: Conference Calendar
version: 0.0.1

output:
  variables:
    - id: calendar
      view: table
      path: calendar.csv

batches:
  - folder: batches/standard

scripts:
  - command: python run.py {output_folder}
